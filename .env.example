# Optional. Used only for explicit global geocoding fallback in Risk Lens.
# The app works without this token and still uses Leaflet/OpenStreetMap for map rendering.
NEXT_PUBLIC_MAPBOX_GEOCODING_TOKEN=
